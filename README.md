# Telegram Media Archiver Bot

Telegram bot for downloading and delivering media with yt-dlp.

## Running locally

On Windows, double-click `run.bat` to set up the virtual environment, install dependencies, and start the bot. Provide `TELEGRAM_BOT_TOKEN` when prompted.

## Tests

Run the automated checks with:

```
python -m pytest
```

